[package]
name = "pigeon-mock"
version = "0.1.0"
edition = "2021"

[lib]
name = "pigeon"

[dependencies]
graphql = { git = "https://github.com/getlipa/wild", tag = "v1.24.0" }
honeybadger-mock = { path = "../honeybadger" }
lazy_static = "1.4.0"
